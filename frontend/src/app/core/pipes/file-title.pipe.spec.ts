import { FileTitlePipe } from './file-title.pipe';

describe('FileTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new FileTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
